---
- hosts: all
  roles:
    - role: 'roles/commons

- hosts: frontends
  tasks:
  - name: Deploy HAProxy
  - include_role: 
    name: frontends

- hosts: apps
  tasks: 
  - name: Deploy Tomcat
  - include_role: 
    name: apps

- hosts: appdbs
  tasks:
  - name: Deploy PostgreSQL
  - include_role:
    name: appdbs
